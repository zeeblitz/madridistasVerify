discord:
  token: ${DISCORD_BOT_TOKEN}

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/discord_bot
    username: postgres
    password: passw0rd!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true